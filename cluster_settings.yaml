# Default parameters for jobs
__default__:
  threads: 1
  vmem: 4G

# Parameters specific to lastdb
lastdb:
  threads: 4
  vmem: 8G

# Parameters for lastal
# total vmem must be at least as the suffix array for the genome generated by lastdb 
lastal:
  threads: 1
  vmem: 16G 
